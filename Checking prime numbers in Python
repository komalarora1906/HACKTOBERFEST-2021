def prime(numbers):
    cnt=0
    for num in numbers:
        for x in range(2,num):
            if num % x == 0:
                break
        else:
            cnt+=1
    return cnt

x=prime([10,20,30])
print(x)
